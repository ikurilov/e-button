import { SlideTypeTitlePipe } from './slide-type-title.pipe';

describe('SlideTypeTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SlideTypeTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
